Future<String> saveJsonFile(String jsonString) async {
  return jsonString; // On web, just return the string
}

Future<String> saveCsvFile(String csvString) async {
  return csvString; // On web, just return the string
}
